# @package _global_
defaults:
  - override /data: mnist
  - override /model: glow
  - override /level_priors: mnist

# MNIST Specific Overrides
model:
  num_levels: 3
  steps_per_level: [32, 32, 32]
  hidden_channels: 256