# @package _global_
defaults:
  - override /data: cifar10
  - override /model: glow
  - override /level_priors: cifar10

model:
  num_levels: 3
  steps_per_level: [3, 3, 3]
  num_blocks: 8
  hidden_channels: 256
  dropout: 0.1

wandb:
  group: "CIFAR10"