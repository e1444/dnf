# @package _global_
defaults:
  - override /data: cifar10
  - override /model: glow
  - override /level_priors: cifar10

model:
  num_levels: 3
  steps_per_level: [32, 32, 32]
  hidden_channels: 256

wandb:
  group: "CIFAR10"