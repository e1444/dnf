# @package _global_
defaults:
  - override /data: cifar10
  - override /model: glow
  - override /level_priors: cifar10

model:
  num_levels: 3
  steps_per_level: [24, 24, 24]
  hidden_channels: 512

wandb:
  group: "CIFAR10"