defaults:
  - default
  - _self_
  - override /scheduler@scheduler: cosine

epochs: 10
lr: 1e-3
lr_prior: [1e-5, 1e-5, 1e-5]
lr_prior_channel: [1e-5, 1e-5, 1e-5]
lr_prior_spatial: [0, 0, 0]

# High Regularization
r_logdet: 0
r_aniso: 5e0
label_smoothing: 0.1

# Loss Weights
l_lambda: 0.7     # NLL weight (1.0 - lambda = CE weight)
em_steps: 0

# Disable EMA for warmup (phase 1)
use_ema: false